<div{{ attributes }}>
    {{ title_prefix }}
    {% if label %}
        <h2{{ title_attributes.addClass("block-title no-margin") }}>{{ label }}</h2>
    {% endif %}
    {{ title_suffix }}
    {% block content %}
        {{ content }}
    {% endblock %}
</div>
